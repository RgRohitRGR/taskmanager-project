import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  // Optional: Set a specific host/port if needed, but defaults are usually fine
  // server: {
  //   port: 3000, 
  //   host: true 
  // }
})